import 'package:flutter/material.dart';

class SettingsPageIsAuthorized extends StatelessWidget {
  const SettingsPageIsAuthorized({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Авторизован'),
    );
  }
}
